export class Skill{
    id: number;
    level: number;
    name: string;

    constructor(id: number, level: number, name: string){
        this.id = id;
        this.level = level;
        this.name = name;
    }

}